{
  "name": "desafio-final",
  "version": "1.0.0",
  "source": "src/index.html",
  "license": "MIT",
  "dependencies": {
    "gh-pages": "^5.0.0"
  },
  "devDependencies": {
    "@parcel/transformer-image": "2.0.0-rc.0",
    "parcel": "^2.0.0-rc.0"
  },
  "scripts": {
    "dev": "parcel serve ./src/index.html",
    "build": "parcel build ./src/index.html --dist-dir build --public-url /dwf-m5-desafio-final/",
    "deploy": "gh-pages -d build ",
    "build-and-deploy": "yarn build && yarn deploy"
  }
}
